build:
  python_version: "3.10"
  python_packages:
    - torch
    - torchvision
    - diffusers
    - transformers
    - accelerate
    - safetensors
    # Remove placeholder text "other packages you need..."
predict: "predict.py:Predictor"
